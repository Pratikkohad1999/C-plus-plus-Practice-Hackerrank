#include <bits/stdc++.h>

using namespace std;
// Program starts
int main()
{
    int apple;
    cin >> apple;
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
    string numbers[9] = {"one","two","three","four","five","six","seven","eight","nine"};
    if(apple>0 && apple<10)
    {
        cout<<numbers[apple-1];
    }
    else 
    {
        cout<<"Greater than 9";
    }
    return 0;
}
// Program ends

